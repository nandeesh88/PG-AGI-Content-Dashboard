import React from 'react'
import { render } from '@testing-library/react'

describe('SearchBar (placeholder)', () => {
	it('has a passing placeholder test so suite is not empty', () => {
		expect(true).toBe(true)
	})
})
